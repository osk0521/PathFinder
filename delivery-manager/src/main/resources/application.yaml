spring:
  application:
    name: delivery-manager-service
  config:
    import: "configserver:http://localhost:19000"
  cloud:
    config:
      name: delivery-manager-service
      profile: dev
      fail-fast: true

